> **Status: planning baseline, 2026-08-30.** Written by the design workflow (3 proposals → 3 judges → synthesis → 2 adversarial critics → revision 2) reviewed by Claude, and walked through with Matt on 2026-08-31 — D35 resolves every open decision, so the baseline stands approved for build. Provenance in `docs/design-review/`, evidence in `docs/research/`. Change decisions through `docs/DECISIONS.md`; keep this file current per arc.

# Roadmap — arcs ≈ sessions ≈ minor versions

Each arc: implement → adversarial review → fix → report → user approves the commit and tag. CHANGELOG entry per arc. Every source feature lands in the arc whose tier first renders it.

## Arc 1 — v0.1.0 "the grid lights up"
*Split 1a (core seam — **Fable**, `docs/briefs/arc-1a.md`) / 1b (cpu + htop + retrowave — **Opus**, `docs/briefs/arc-1b.md`); later arcs get a brief at arc start (D36).*
**Goal:** a screenshot-worthy Overview page in Ptyxis — htop meters plus 32 gradient-coloured core bars in CCD blocks and a big clock, in retrowave and modern — on top of the core seam, with demo mode, the testkit and a measured throughput number. No GPU, no journal, no process table, no effects.
**Deliverables**
- [x] Workspace scaffold: six crates, workspace pins (explicit ratatui-core/-widgets features, crossterm only in app/cli), `rust-version = 1.88`, `clippy.toml` bans on `ratatui::{init,try_init,run,restore}`, `deny.toml` (ISC + CDLA allowed, tokio assertion over four features), `ci.yml` (fmt, clippy -D warnings, test, doc -D warnings, MSRV, per-crate check, feature matrix, deny, tree -d, audit)
- [x] `gridwatch-store`: Ts/Clock, Key/Label/Datum, `RecordValue` + `KeyMeta` catalogue with `lookup()`, Ring (VecDeque), series + retention, `Store::apply` + read API + `resample`, `Msg`/`Batch`/`ControlMsg`/`InputEvent` and the three channels, Source/Sampler/SourceCtx/Demand/Level/Cadence/Control/SourceStatus, `demo::Synth`, catalogue for `sys` and `cpu` (incl. `sensor.temp_c{k10temp:*}`)
- [x] `gridwatch-ui`: Component/Manifest (sources + optional_sources)/ComponentDef/Registry/BuildCx/Command/Action, Footprint/Tier (cumulative, `zoom_only`)/Chrome + `view` resolution + `demand`, `View` tree + `Span` + the default `Renderer` for Text/KeyValue/Gauge/Segmented/Bars/Sparkline/Chart/Table/BigNumber/Stack/Custom parameterised by `Theme::widgets`, view-tree YAML snapshots, `schema/view.schema.json` + `schema/{config,layout,theme}.schema.json` with fixture validation in CI, theme `class` and `[widgets]` parsed (`[ambient]` ignored with one warning until arc 4), layout engine (tracks/thresholds/solve/hit/unit_at/focus_dir, 12×6, size-derived Configured/Dense/Stack with hysteresis, chip fallback, too-small notice; the pure edit ops and their proptests land here — the edit *mode* is arc 4), Theme loader v1 (roles, `$palette`, Oklab gradient LUTs, glyph tiers, borders, title styles, ColorMode ladder + mono; `[flourish]`/`[effects]`/`inherits` parsed and ignored with a warning), built-in `modern`/`retrowave`/`mono`, widgets stacked_bar/vbars/big_number/chip/kv_table/sparkline_ext, overlay (help, stats HUD, too-small), `dump::cells` (RLE styled) + `dump::ansi`, testkit (`snapshot_matrix!`, `role_swatch!`, `assert_never_panics`, `assert_min_tier_fits`, `real_grid_sizes()` for 250×70 configured and 120×40 dense)
- [x] `gridwatch-sources`: supervisor (catch_unwind, backoff, restart counter), `cpu` (procfs: stat breakdown with guest subtraction, meminfo formulas, loadavg, PSI, topology, freq, k10temp Tccd by label; no process table yet)
- [x] `gridwatch-components`: `clock` template (Chrome::Borderless honoured), `sources` tile, `htop` tiers tiny/big-number/meters/cores (read-only)
- [x] `gridwatch-app`: own terminal init/restore, PanicPolicy hook, stderr dup2, input thread with crossterm→InputEvent conversion, `run<B: Backend>`, loop with drain order + per-source generation dirty gating, 30/60 fps, pages/hotkeys, Tab + hjkl focus, Enter/Esc capture, `z` zoom, `d` dense override, `t` theme cycle, `space` pause, Bg painted first, config.toml + layout.toml loading with spans and the options-disjointness check, `F12` HUD, `S` screenshot
- [x] `gridwatch` CLI: `run [--demo [--seed N]|--page|--theme|--fps|--color|--no-mouse|--stats]`, `shot --format ansi`, `config check|default`
- [x] Tests: store unit tests (ring, resample, apply, control-under-full-data), tracks-vs-ratatui oracle, `thresholds()` for the default grid, proptests (mode monotonic with hysteresis, no-panic sweep), snapshot matrix for clock/htop at real sizes (modern) + role swatches for all three themes, default-layout no-chip assertion at 120×40 and 250×70, htop formulas against `fixtures/procfs`
- [x] `scripts/perf/measure.sh` (per-thread pidstat, task-summed voluntary switches, Δwchar from /proc/<pid>/io, nvidia-smi pmon, pw-top) and `--stats-log`; named threads; `EnableFocusChange` → unfocused throttle (`[perf] unfocused_fps = 2`, level → Hidden and detail → Meters); zero-poll `SourceCtx::sleep_until` on `[perf] phase_ms = 250`; render cache keyed by source generations; frame coalescing
- [x] `docs/PERFORMANCE.md` first measured rows: p50/p95 frame time, changed cells, bytes/frame and wake-ups/s at 250×70; 30-second Ptyxis glyph check (rounded corners, eighth blocks, braille, octant) recorded in `docs/THEMES.md`; `docs/PARITY.md` htop section; README with an ANSI dump and a captured PNG
**Acceptance:** `cargo test --workspace` passes with no hardware; `gridwatch --demo` and `gridwatch` (live) show the Overview page (htop meters + cores + clock + the `sources` tile in the `amp` slot; other placements as placeholder chips) in both themes at 250×70 with p95 frame time <8 ms; at 120×40 the page renders in dense mode with every tile above chip level; clippy/fmt/doc/MSRV/deny/per-crate check green.
**Performance gate:** `scripts/perf/measure.sh` exists and P1, P4, P5, P6, P8, P18, P19, P21 are green on torch (first measured rows land in `docs/PERFORMANCE.md`); VTE focus reporting verified so the unfocused throttle works. **Read the status line below before treating this row as met — P4 and P21 are owed against the 1b build.**
**Status 2026-09-01 (arc 1 committed and pushed, CI green; `v0.1.0` untagged):** every deliverable above is implemented and gated. P1, P5, P6 (with the HUD-vs-Δ`wchar` cross-check at 1.99 %), P8, P17, P18 and P19 are measured green under a 250×70 pty on an idle box; **P4, P21 and the Ptyxis rows P9/P10 are owed** — a pty sends no focus events — as is a re-take at Matt's real window size with the game running. The Ptyxis glyph check and the README PNG are the two rows a machine cannot tick. Two startup defects found by running the binary by hand were fixed after the review: a non-tty exited silently (stderr was redirected before the terminal opened) and a too-small terminal drew a blank screen (§6's notice existed but nothing called it).
**Risks:** VTE throughput below expectation (the HUD decides whether 60 fps stays opt-in); the trimmed theme loader tempting scope creep back in (inherits/overrides are explicitly arc 3).

## Arc 2 — v0.2.0 "history and the GPU"
*Brief: `docs/briefs/arc-2.md` (D47, 2026-09-01) — two Opus sessions, **2a** journal + pid scan + htop table + tooling, then **2b** the GPU.*
**Goal:** the GPU component with nvtop's rolling charts, the process table, and journal record/replay with a determinism test and CI screenshots.
**Deliverables**
- [x] Journal (2a, 2026-09-01): RecordValue to_json/decode, Recorder tee, Replay + JournalSource, virtual clock, round-trip test over every catalogued Record type, `--record F`, `--replay F --speed N`, `--record-input`, `--tables on|off`, `shot --replay F --at SECS`; `schema/journal.schema.json` validates the fixtures
- [x] (2b, 2026-09-01) `gpu` source: NVML static probe, fast/slow tiers, power trace, PCIe byte counters, NotSupported pruning, LibRmVersionMismatch/Libloading states, nvidia-smi fallback tier, const spec table cross-check; keys labelled `{0}`; Synth GPU data
- [x] (2b) `gpu` component tiers badge/gauges/header/charts/**procs**/`full` (mins 8×3 / 24×5 / 56×8 / 56×12 / 56×18; resample-driven 10-minute charts in a 4–8-row band, selectable series, reverse, effective-load, spec column; `procs` = top-N GPU process table with the grid default `PID DEV TYPE GPU GPU MEM CPU HOST MEM Command` (`USER`, `ENC`, `DEC` via `columns`), default sort `gpu_mem`, `table_rows = 10`, zoom fills the body; select, `</>`/`F6` sort and `I` only — `/`, `F9` and Actions are arc 8); manifest `optional_sources = [cpu]`, `demand(procs) = Detail::Table`
- [x] (2b; `samples(Power)` gated on a *visible* gpu tile rather than `header`+ — D49) `gpu` source process rows: v3 graphics + compute lists (`InsufficientSize` retried) overlaid with `process_utilization_stats(last_seen)` using nvtop's carry-forward timestamp, gated on `Detail::Table`; fans %/RPM at 5 s, `samples(Power)` while a gpu tile is visible and the process rows on a 2 s grid so the slow tier stays inside P11 with process rows (measured, see PERFORMANCE); `gpu.procs{dev}` key; joined with `proc.table` in the component's `tick` with a per-PID last-known cache
- [x] (2a) `cpu` source pid-level process scan at `Detail::Table` (`stat`, `statm`, dir `st_uid`, `cmdline` on first sight; deltas keyed by `(pid, starttime)`; `sys.pid_digits`; 3 s grid / 1.5 s focused); htop `table` tier (min 56×18; top-N with the grid default `PID RES SHR S CPU% MEM% TIME+ Command` — htop's full Main screen `PID USER PRI NI VIRT RES SHR S CPU% MEM% TIME+ Command` via `columns` and in `full` — at htop's printed widths incl. auto-width CPU%, `Row_printKBytes` regimes, state colours, default sort `cpu`, `hide_userland_threads = true`, `table_rows = 10`, gridwatch's drop order with `command_min = 20`, zoom fills the body); §8.1 snapshot tests at 122×31, 80×20, 59×18 and 248×66
- [x] (2a: `proc.table`; 2b: `gpu.procs`) `demo::Synth` 32-process `proc.table` + `gpu.procs` set (a game at 12.5 GiB / 17 % SM, a shell, a browser, kernel threads, one `Both G+C` process, VIRT/RES values hitting all three KiB regimes)
- [x] (2a) `dump::svg` + `shot --format svg`; CI screenshot job regenerating `docs/img/*.svg` and failing on drift; `component list|info`, `keys` → `docs/COMPONENTS.md`, `docs/KEYS.md`
- [~] Fixtures: `torch-idle.jsonl` re-recorded with both sources (2b); **`torch-game.jsonl` owed — no game was running during 2a or 2b, and an agent must not start one**; determinism test replaying through `run<TestBackend>` twice with identical frame hashes (2a, gpu keys covered by the re-recorded fixture); `docs/PARITY.md` nvtop section (2b, done)
**Acceptance:** gpu header/gauge and process numbers match `nvtop --snapshot` within a tick on torch; the Overview at 250×70 shows 10-row tables in both 6x3 tiles, the 120×40 dense layout shows 5-row tables, and the zoomed gpu tile shows CPU/HOST MEM/Command for the game; charts show 10 minutes of history; replay of a fixture is byte-identical across two runs; README images come from CI.
**Performance gate:** P11 (≤ 6 ms/s with the Overview's `procs` tile visible, per-class sum shown in the `sources` tile), P12, P13, P15, P17 (1 h) green per `docs/PERFORMANCE.md`; the process rows add ≈ 2 ms/s and nothing when no gpu tile is at `procs`.
**Risks:** NVML field quirks on driver 610 (per-field probing); journal size with tables (tables off by default).

## Arc 3 — v0.3.0 "the signature tile"
*Brief: `docs/briefs/arc-3.md` (D50, 2026-09-02) — two Opus sessions, **3a** pins source + component + alert overlay + `synth-overload` fixture + PARITY, then **3b** hot reload, theme loader v2 (`terminal`, `phosphor-green`), staleness, `doctor`. astral-watch stays pinned at `dce7eee`; v0.8.0 is not a prerequisite (D50 §1).*
**Goal:** astral-watch parity as a first-class component with the cross-page alarm overlay, plus the degraded-mode story.
**Deliverables**
- [x] (3a, 2026-09-02) `pins` source (git-pinned astral-watch `dce7eee`, auto exporter → i2c; Lifecycle bridge with self-tracked active set, TelemetryLost feed, redetect); `pins` keys; Synth pin data with scripted overload; CSV tail deferred to arc 8
- [x] (3a) `pins` component tiers watts-badge/mini-bars/bars/trend/full (single card; tui.rs parity incl. device header from the gpu source's keys — PCIe from `gpu.pcie_gen/width`, not sysfs — braille trend chart, scrollable log, `p`/`r`/`+`/`−` keys)
- [x] (3a) AlertLog + control-channel alerts + overlay banner/toasts/ack + `alerts` tile + `A` overlay; blink-free alert pulse on the heartbeat (tachyonfx comes in arc 4)
- [x] (3a, D51) **Record the alerting stance (DECISIONS entry):** gridwatch is a viewer — the astral-watch service owns out-of-band alerting; the optional unfocused `notify-send` on Crit stays in BACKLOG unless promoted here (D39)
- [x] (3b, 2026-09-02, D53) Capability probe (`CapSet`, ≤200 ms), `Manifest.requires/optional`, placeholder tiles with fix text, `gridwatch doctor [--offline]` with the sources' live probes, staleness dimming + `STALE` badge
- [ ] **Owed to a human (3b):** P1/P5/P6 re-taken with the *live* pins source and `doctor`'s live table — both open `/dev/i2c-*` (the 3b rows in PERFORMANCE.md are `--demo`)
- [x] (3b, 2026-09-02, D52/D53) 1 Hz mtime hot reload for config/layout/theme with instance diffing and error toasts (`file:line:col`); theme loader v2 (`inherits`, `[components.<kind>]` overrides, WCAG warn gate, terminal colour names); `terminal` and `phosphor-green` themes; `T` reloads the theme
- [ ] **Was a pre-req (D35), demoted by D50 §1 — arc 3 pins `dce7eee` and needs none of the PRs; do this whenever convenient:** in a separate astral-watch session, open the three PRs (`cli`/`notify` feature gating, `log` facade, `Lifecycle::active()`) and cut the v0.8.0 tag; gridwatch then pins the tag instead of the rev. **Decide crates.io in that same session** — a git dependency can never be published, so if gridwatch is ever to reach crates.io, `cargo publish` astral-watch 0.8.0 then (D39)
- [x] (3a) Fixtures: `synth-overload.jsonl`; replay test asserts `pins/overload` raised at the scripted Ts and the banner renders on page 2; `docs/PARITY.md` astral-watch section
**Performance gate:** P14 green (≤ 2 i2c transactions/s, ≤ 1 % CPU); the banner adds no steady-state redraws.
**Acceptance:** pins tile matches astral-watch's TUI feature list per PARITY.md; a synthetic overload raises the red banner on every page and resolves with a green toast; running beside the root logger shows no corrupted readings; `gridwatch doctor` lists every capability with reasons.
**Risks:** astral-watch API drift before the tag (pin rev); i2c contention latency (≥500 ms, exporter preferred); GPU idle zeros (TelemetryLost path tested via replay).

## Arc 4 — v0.4.0 "rearrange and shine"
**Goal:** edit mode, the effects layer, and the `matrix` theme as the first showcase-class theme. Realistically two sessions: **4a** edit mode, **4b** effects + matrix. Matrix's theme-vs-usability balance is explicitly expected to iterate here (Matt, 2026-08-31) — budget review time for tuning `fade_s`/`trail_ms`/`sweep_s`/density against real use, and treat D31/D34's numbers as starting points, not commitments.
**Deliverables**
- [x] (4a, 2026-09-02, D54) Edit mode state machine over pure page ops: HJKL move, Ctrl-hjkl resize (direction carries the sign), `s` footprint cycle, `S` swap, `a` picker (instances or `kind:` shorthand, first-fit insert, typed filter), `x` remove, undo/redo, red-ghost collisions, dotted unit grid, mouse drag/corner resize
- [x] (4a) `w` save via toml_edit into `layout.toml` only (comments preserved outside `place`, atomic rename, re-parse check, self-write hash through the watcher's ignore slot)
- [x] (4b, 2026-09-02, D54) tachyonfx effect hooks (startup sweep/fade, theme_swap fade/dissolve, focus fade, alert hsl pulse) with the `budget_ms` watchdog and `--no-effects`; `[effects]` now consumed (no CRT ambient: the matrix layer is the ambient layer)
- [x] (4b) Flourishes: gradient titles (arc 1), sun + grid floor in empty slots, big clock pixel modes; `phosphor-amber`; WCAG `contrast.autofix`
- [x] (4b, 2026-09-02, D54 — S1 beside the game, S3, S5 with the game focused, S6's eyeball row and the katakana glyph check are Matt's; see PERFORMANCE) **`matrix` theme (D28):** `class = "showcase"`, the ambient layer as a hand-written post-render pass (D54: not a tachyonfx `effect_fn`), `matrix_rain` (seeded droplets, head/trail through the `Rain` gradient, half-width katakana with `rain = "ascii"` fallback), the **rain-lit renderer** (D31: per-cell `lit` brightness; a droplet head passing a content cell shows the rain glyph for one frame then the module's own character at full brightness, which fades through its own colour to the floor over `fade_s`; a dense **sweep** every `sweep_s` re-renders the whole page in one fall; `relight_on_update` re-lights changed cells so live values stay bright), readability pins (focused tile, alerting tiles, tab bar, banner/toasts, key bar, hover and `reveal_ms` after a key; `V` re-lights the page, `L` locks all lit), a `fade_in` startup (no `rain_fill`), freeze on `FocusLost`/pause with the UI still drawn from the frozen state, the governor (fps → density → sweep period → gutters-only, 2 s down / 30 s up, HUD state), determinism + readability + fade + sweep + re-light tests, a 30-second Ptyxis glyph check that katakana render one cell wide (Noto Sans Mono CJK JP fallback — **Matt's**)
- [x] (4a) Edit-op proptests extended to the mouse paths: `unit_at ∘ solve` over both grid modes and every body size; `drag_ops_never_break_the_grid` over random press/drag units (move and corner resize); the drag/corner paths through the shell as example tests
**Acceptance:** a layout edited in-app round-trips through `layout.toml` with comments intact and `config.toml` untouched; effects stay within budget on the HUD; retrowave vs modern screenshots regenerated in CI; under `matrix` the rain runs at 24 fps on the Overview with the game running, the whole page fades between sweeps and is re-rendered by each fall, a GPU-utilisation change re-lights exactly its cells, a synthetic pins overload is readable through it, tabbing to the game drops gridwatch to P4 within one frame, and the governor engages when the window is stretched to a size the terminal cannot sustain.
**Performance gate:** P20; idle edit mode meets P8; S1–S7 under `matrix` focused, S5 = P4 with the game focused.
**Risks:** effect cost on VTE (area scoping + watchdog); toml_edit formatting loss on removed items (documented).

## Arc 5 — v0.5.0 "it moves"
**Goal:** the audio visualizer and the sensors tile.
**Deliverables**
- [x] `audio` source: pw-record supervisor (latency 1024 default, `low_latency` opt-in, EOF-only respawn, passive-silence rule, kill after 10 s hidden), rtrb ring, DSP thread (dual FFT 8192/2048, 64 log bands, floor/tilt, scope, RMS/peak, 2 Hz idle publishing), `pw-dump` sink enumeration, Synth stereo mix — 5a, 2026-09-02
- [x] `audio` component: ballistics presets (winamp/cava), tiers vu/mini/scope/spectrum/full, octant-or-braille scope, global sink picker, `Animated{fps}` while visible with `fps = 30` default — 5a, 2026-09-02 (the picker is per tile and sends the first `Control::Domain`)
- [x] `sensors` source (hwmon walker, PSI, cpufreq, RAPL gate; takes over `sensor.temp_c{k10temp:*}` from cpu) and component tiers hottest/strip/table/chart/full — 5b, 2026-09-02 (PSI and cpufreq stay the cpu source's keys; the tile reads them)
- [x] DSP tests (0 dBFS sine, 50 Hz single-band); record `fixtures/journals/torch-audio.jsonl`; hardware-gated `#[ignore]` capture test; HUD measurement of the animated cell at 30 and 60 fps — 5a, 2026-09-02 (the fixture is the silence path: nothing was playing; the "reacts to Firefox/game audio" row stays Matt's)
**Acceptance:** live spectrum reacts to Firefox/game audio within ~30 ms; CPU within P2/P3 (≤ 6 % of one core with the visualizer at 30 fps, ≤ 10 % at 60 — PERFORMANCE.md is the gate; 5a measured 4.72 % / 6.05 %); `pw-top -b -n 1` shows the graph quantum unchanged at the default latency with no game running and ≤512 only with `low_latency` while the widget is visible.
**Performance gate:** P2, P3, P7, P9, P10, P16 — including the Ptyxis CPU/SM delta with the visualizer visible beside the game.
**Risks:** pw-record flag drift across PipeWire versions (version check + `--help` parse); eighth-block coverage in the user's actual font (ascii tier fallback).

## Arc 6 — v0.6.0 "Winamp"
**Goal:** the MPRIS now-playing component in classic-skin form.
**Deliverables**
- [x] `mpris` async source on the private tokio thread: proxies, discovery, per-player tasks, position model, art fetch/decode, `MediaCmd` control, Synth fake player with embedded PNG — 6a, 2026-09-02 (the fake-bus model replaces per-player tasks: one connection, one poll grid; art on a blocking task)
- [x] `winamp` component: Custom chrome, `optional_sources = [audio]` with static vis fallback, tiers status/shade/main/main+art/full, marquee from `cx.now`, big digits, 19-band vis from `audio.bands`, kbps/kHz from `audio.sink`, posbar/volume/transport, EQ weighting, local playlist history, halfblock art — 6a, 2026-09-02 (`Chrome::Themed`, not Custom: the shell owns the frame — D56 amendment)
- [x] Recorded `a{sv}` metadata fixtures (Firefox/YouTube, a single-element page) for decoding tests — 6a, 2026-09-02 (`fixtures/mpris/`: Firefox/YouTube, a stream, no title, two artists)
**Acceptance:** Firefox playback controls work from the tile; track changes update within one poll; stream mode when `mpris:length` is absent; replay determinism holds with the marquee running; the tile renders on a page without the audio component.
**Risks:** Firefox Position=0 on multi-element pages (stream mode); zbus/tokio feature interplay (asserted by the deny job).

## Arc 7 — v0.7.0 "wired"
**Goal:** the network component (Tier 0) and generic alert rules.
**Deliverables**
- [x] `net` source: all-interface rates/link/addrs/route/DNS, connection table at `Detail::Table`, `net-probe` (ICMP DGRAM → TCP fallback) with ring stats, optional neli-wifi and NM fallback, public IP opt-in — 7a, 2026-09-02 (Wi-Fi is fixture-shaped only: `wlp7s0` is down on torch; the public-IP lookup is wired as an option and not implemented — both owed)
- [x] `net` component tiers rates/sparks/table/conns/full with instance-level interface filters and rDNS opt-in — 7a, 2026-09-02 (rDNS is an accepted option, not yet implemented)
- [x] `[[rules]]` engine (name-indexed, hold/clear, metric RHS, label wildcards, absent) + `config check` for rules; default rules for gpu-hot, cpu-hot, nvme-crit, link-down — 7b, 2026-09-02
- [ ] Capability badge and design note for the future `gridwatch-netd` helper (per-process bandwidth)
**Acceptance:** rates match `ip -s link` deltas; probes show gateway ~1–4 ms; own-process connections attributed; a rule raises and clears with hysteresis in replay.
**Risks:** Wi-Fi path untestable until wlp7s0 is connected (fixtures); mDNS stalls (async resolver only).

## Arc 8 — v0.8.0 "full parity"
**Brief:** `docs/briefs/arc-8.md` (D58, written 2026-09-02).
**Goal:** htop and nvtop interactive parity and the remaining pins modes, checked against PARITY.md.
**Deliverables**
- [x] htop full tier: screens/tabs, tree view, search/filter, sort keys, tags, follow, kernel/user thread toggles, F-key bar; actions kill/renice/affinity/ioprio via `Action`s with confirm + `readonly` — 8a, 2026-09-02
- [x] gpu `full` tier (zoom-only): `F6` sort by any nvtop criterion, `+`/`−`, `F9` signal menu via `Action`, Power sub-panel showing pins under board power; htop `full`: I/O screen columns and htop's gated files at `Detail::Columns`, `H` userland threads with the `task/` walk, `/` search, tree, filter, tags; the executor thread and every `Action` land here — 8a, 2026-09-02 (the I/O screen reads the block-layer counters; the `task/` walk for `H` is the demand path, and the thread rows themselves are arc 9)
- [~] pins CSV tail mode verified against a running service; exporter mode verified; multi-card tabs — **none of this could be done here.** The CSV comes from the service that owns the i2c bus and the exporter needs it running, so both live rows are **owed to Matt** (`MACHINE.md` has the steps); multi-card was deferred with reason to a brief of its own (D58 amendment 6, `BACKLOG.md`) because every `pins.*` key is labelled by pin number alone, so a card dimension is a change to the whole key seam — and torch has one card, so an agent could not have verified a line of it
- [x] `exec` plugin host — 8b, 2026-09-02: the schemas and the supervisor first, then the wiring — `[[plugins]]` config, the manifest registered as a placeable `<id>.<kind>`, the tile, samples as ordinary batches, `config check`'s listing, `shot --config`, and the two bounds P22 turned up (a 500 msgs/s read budget and a 50 %-for-10 s runaway check) — the public plugin API: JSON-lines protocol (hello/manifest, samples, render → view tree, keys, commands, status) validated by `schema/exec.schema.json` + `schema/manifest.schema.json`, supervised like a source, an example Python plugin under `plugins/examples/`, `docs/PLUGINS.md`; a plugin may be a source, a component, or both; **security posture** (D39): schema-validated input only, max line length, unknown kinds rejected, CPU/RSS caps with kill-on-runaway, no shell interpretation of plugin commands
- [x] `docs/PARITY.md` diff: every in-scope row checked — 8a walked the htop and nvtop sections and ticked or explained each one (the deliberate deviations are written into the rows: `k` is the vi cursor key, htop's configurable screen *set* is out, the signal menu is nine signals not thirty-one, the DRM `GPU%` column the proprietary driver cannot fill). The two rows still saying "arc 8" are the pins ones above, which are Matt's and the deferred brief's
**Acceptance:** htop muscle memory transfers for the listed keys; EPERM surfaces as a toast; nvtop header/process numbers match `nvtop --snapshot` within a tick; no in-scope PARITY.md row unchecked.
**Performance gate:** zoomed `full` tiers stay inside P15 (`smaps_rollup` only with the column on) and P19; the plugin host inside **P22** — measured with the example plugin at 1 Hz *and* with one that floods, because the flooding row is the one that failed first.
**Risks:** privilege edge cases (documented, never escalated).

## Arc 9 — v0.9.0 "ship it"
**Goal:** packaging, docs and 1.0 readiness.
**Deliverables**
- [x] `release.yml` gnu + musl tarballs, nfpm deb/rpm, AUR PKGBUILD, Nix flake, udev RAPL rule (optional) — 9b, 2026-09-03. **Run before it was needed** (`gh workflow run release.yml`, green first time): both libcs build and are *asserted* — the binary runs a real `shot`, the tarball must list what it claims, the deb must install, run and remove, and musl must report `✗ Nvml` rather than pretend. The PKGBUILD and the flake are written and marked **UNVERIFIED** in their first lines: no `makepkg` or `nix` on this machine
- [x] `gridwatch theme import` (alacritty/wezterm/base16) — 9a, 2026-09-02: reads the format from the file's contents, derives the roles a foreign scheme cannot carry, **loads what it wrote** and prints the WCAG report, writes a file and never applies one; a fixture per format renders a real frame in the tests
- [x] Docs complete (ARCHITECTURE, ADDING-A-COMPONENT, COMPONENTS, THEMES, LAYOUT, KEYBINDINGS, PERFORMANCE, PARITY, CONTRIBUTING), README with per-theme screenshots, THIRD_PARTY.md — 9a, 2026-09-02. `KEYBINDINGS.md` and `THIRD_PARTY.md` are **generated** and drift-checked, as is the README's own embedded frame (it had been claiming four shipped tiles were still to come); `ADDING-A-COMPONENT.md` was written by following §15 against `sensors`, which turned up two wrong claims about the real API
- [x] crates.io publication path decided (blocked on astral-watch 0.8.0), `cargo install --git` documented — 9b, 2026-09-03: the decision is recorded (D59 9b amendment 4) and `cargo install --git --locked` was **run from a clean clone with no `.cargo/config.toml`** and the installed binary drew a frame
- [x] Bench suite (criterion) for apply/resample/full-frame render committed with baseline numbers — 9a, 2026-09-02: `cargo bench -p gridwatch-app`, deliberately **not** a gate (D59 seam 3), baselines in PERFORMANCE.md — apply 2.48 µs, an uncached 250x70 frame 513 µs against P19's 8 ms, theme load 26.5 µs
**Acceptance:** a fresh Ubuntu container with only build-essential + pkg-config builds every feature; packaged binary runs on torch with all seven components live.
**Owed to Matt (9b):** the container build — `MACHINE.md` says not to launch heavy jobs beside the game, and it is his acceptance criterion to take; the packaged binary run on torch with all seven components *live*; `makepkg -si` on an Arch machine and `nix build` on one with Nix; and the **version number** — `Cargo.toml` still says `0.1.0`, so the installed binary reports `gridwatch 0.1.0` while the README says v0.9.0 is in progress. Bumping it is part of cutting a release, which is his.
**Risks:** musl + NVML dlopen path (runtime test in release job).

## Arc 10 — v0.10.0 "the debt"
*D60, 2026-09-04. Six items that nine adversarial reviews found, verified as real, and wrote into `BACKLOG.md` without ever scheduling. Split by risk (`CLAUDE.md`: risky halves revert alone), not by provability: **10a** touches nothing below `gridwatch-components`/`app`, **10b** changes the store's hot path and adds a `/proc` walk with a performance row.*
**Goal:** the places where the program is quietly wrong rather than visibly broken — a check that passes when it should fail, a key that moves the wrong distance, an instrument that changes what it measures, a theme that renders a true value unreadably, a map that grows without bound, and a toggle that changes what is asked for but not what is shown.
**Deliverables**
- [x] **(10a, 2026-09-04) `config check` builds every configured component.** Each `[[components]]` instance is built through the registry, so the `deny_unknown_fields` option types the components already declare are actually reached: `sort = "nonsense"` fails the check that exists to catch it, naming the instance, the option and the values it would have accepted. A build failure is a non-zero exit, not a warning line.
- [x] **(10a, 2026-09-04) Table paging follows the body that is drawn.** `htop::page_rows` and `gpu::on_key` read `cx.tier` and `cx.zoomed` (D58 amendment 7 — never infer the tier from `inner`) instead of clamping to `table_rows` and passing `TIER_PROCS`/`false` as literals; **one method owns the full tier's row budget** and both `on_key` and `full::render` call it, so an open search line cannot make the page size drift by a row. `PgUp`/`PgDn`/`Home`/`End` and selection-follow move by what is on screen.
- [x] **(10a, 2026-09-04) The frame counter separates from the changed-cell diff.** Frames, p50/p95 and the redraw-reason counters no longer require the per-frame buffer clone and 17 500-cell compare; the diff is opt-in (`--stats-cells`, and the `F12` HUD, which is a person looking at it). P4 can then be taken with frame counts instead of measuring the product plus its instrument; `PERFORMANCE.md` records the difference the instrument was making.
- [x] **(10a, 2026-09-04) `mono` renders without colour.** A `segmented` widget variant gives each segment its own glyph, so the MEM bar's used/shared/buffers/cache boundaries survive a colourless theme instead of reading as one full bar; The table half of that backlog entry was **examined and closed without a change**: rendering an htop table under `mono` shows `1123M`, `12.5G`, `R`/`S`/`D` and `3h29:40` all printed, so what mono loses there is emphasis, not information (D60 amendment 2). Themes choose form through `[widgets]` (§4.6: a component that named a glyph would be the bug) — `THEMES.md` documents the new key.
- [x] **(10b, 2026-09-04) Retention shrinks what stopped arriving.** A scalar ring is pruned to its **newest point** — never emptied, never removed, because six catalogued scalars are published once and `Store::last` is how every tile reads a current value (the arc-10 review caught the first version deleting them). `Rules::states` *is* evicted for a label that has gone quiet, because losing a state is not losing data. Keyed off store `Ts` and **never** wall clock (arc 2a's determinism test replays a fixture twice and compares frame hashes), swept on a retention boundary rather than per `Store::apply` (whose rules pass measures 24 µs; the store test asserts 500 µs a batch). Bounded for every catalogued key today; the case that is not is `net.*{iface}` on a machine that makes and destroys veth/tun/docker interfaces.
- [x] **(10b, 2026-09-04) htop's `H` lists threads.** The `task/` walk behind `Detail::Columns`, so the toggle changes what is *shown* and not only what is asked for: per-thread rows with their own TID, state and CPU%, htop's own presentation. Arrives with its own `PERFORMANCE.md` row — `task/` over ~640 processes is a large syscall multiple on top of P15 — and its PARITY row ticked with the deviations written down.
- [ ] **Escalated, not built (D60):** `[sources.<id>]` option validation. The only honest fix adds `options: &'static [&'static str]` to `SourceInfo`, which is a §4.3 signature and therefore a **Fable** session's change under D36. The design is written into `BACKLOG.md`; a mistyped `refres_ms` is still silently ignored until then.
**Acceptance:** `gridwatch config check` exits non-zero on a bad component option and says which instance and which option; `PgDn` in the zoomed htop and gpu tables moves by the rows on screen at every terminal size, with and without the search line open; `--stats-log` runs with no per-frame buffer clone and the HUD still shows changed cells; a `mono` snapshot of the MEM meter has more than one distinct glyph across its segments; a replayed fixture is still byte-identical across two runs with eviction on, and a synthetic interface churn stops growing `Store::series`; `H` in the zoomed htop tile lists threads under their process.
**Performance gate:** P18 unchanged (eviction adds nothing to a batch that crosses no boundary); P15 re-measured with the thread walk both off and on, and a new row for its gated cost; P4 re-taken now that it can be measured without the diff; P19 unchanged.
**Status 2026-09-04 (built, gated and pushed; CI green).** All six shipped. Measured rather than asserted: the changed-cell diff cost **169 µs per frame** (0.60 pp of a core at 28 fps, 0.03 % at P4's 2 fps — so it never meaningfully inflated P4 and did inflate P2's 4.72 % by ≈ 0.6 pp); the retention sweep costs **2.3 µs** per batch of 400 series against a control that never sweeps; the `task/` walk costs **+7.6 ms** against the +30 ms P15 had budgeted (5.4 ms → 13.0 ms, 1 807 thread rows on top of 629 processes). Two things the arc found rather than built: the **`mono` table half of its own backlog entry was wrong** — the unit letters, state letters and times are all printed, so what mono loses there is emphasis and not information, and it is closed without a change (D60 amendment 2); and **`Record`/`Vector` eviction needs the catalogue to say which keys have dynamic labels**, a `KeyMeta` change, escalated to a seam session alongside `[sources.<id>]` option validation. Nothing here is owed to a human — every row above was measurable on this machine.
**Risks:** eviction interacting with the charts (a ten-minute chart must not lose a series that is merely quiet — the window is retention, not liveness); the thread walk's cost on a 640-process box; `mono`'s glyph choices needing eyes in Ptyxis, which is owed to a human like every other glyph row.

## Arc 11 — v0.11.0 "the seams say so"
*D61, 2026-09-05. The two items arc 10 escalated instead of building, because each honest fix changes a Rust contract. The contracts were changed by the Fable session that wrote D61 (`SourceDef.options`; `KeyMeta.labels: LabelSet`, `key::labels_dynamic`; `Retention.max_uncatalogued`, `Store::capped`; the per-label sweep and the cap in `Store::apply`) — everything below is breadth against `docs/briefs/arc-11.md`. Split as the debt was: **11a** touches `app` and tests only; **11b** touches what the store now exposes and the docs that describe it.*
**Goal:** the config file cannot be quietly wrong about a source, and the store cannot quietly grow.
**Deliverables**
- [x] **(11a, 2026-09-07) `config check` validates `[sources.<id>]`.** `app::check_sources` beside `check_components`: an unknown key under a known source, or an id that is neither a registered source nor a plugin, is a **failure** (non-zero exit) naming the source, the key and the accepted set (`SourceDef.options`); `[sources.<plugin id>]` is a warning line saying contract 1 delivers no options. The same report is toasted once (Warn) at `run` and re-run on a reload that changes `[sources.*]`; the source still starts and ignores the key.
- [x] **(11a, 2026-09-07) §9's disjointness rule tested for every pair.** `source_and_component_option_names_are_disjoint` walks a table of every component's `OPTION_NAMES` against `registry.source(id).options` for each source its manifest lists, own and optional — not the one hard-coded htop/cpu pair.
- [x] **(11b, 2026-09-07) `KEYS.md` says which labels are dynamic.** `gridwatch keys` gains a `labels` column (`static`/`dynamic`) and the file is regenerated; the drift check still holds.
- [x] **(11b, 2026-09-07) The `sources` tile shows a capped plugin.** `Store::capped(id)` surfaces as a note on the source's row (like `dropped`), so a plugin that hit `max_uncatalogued` is visible rather than merely bounded.
- [x] **(11b, 2026-09-07) The eviction is proven from both ends.** Store tests: a `net.*{veth9a1}` label whose two scalars and `Record` all go quiet is removed after `max_age` while `eno1`'s once-published `net.speed_mbps` survives beside its live `rx_bps`; a `sensor.max_c{nvme:Composite}` published once is kept while `temp_c` for that chip arrives and removed when the chip stops; a `Static` key's quiet label is never removed; a raised rule pins its label; an uncatalogued domain refuses its 513th series and `capped` counts it, and eviction frees the room. **Determinism across an evicting sweep is proven at the store**, by feeding two stores the same batches with a wall-clock gap between them and comparing the whole surviving inventory. The app-level *journal* replay does **not** cross an evicting sweep: every fixture spans about 62 s of store time against a 600 s `max_age`, so nothing in one goes quiet long enough — an evicting replay needs a fixture longer than ten minutes, which does not exist and was not manufactured (arc 11 review).
- [x] **(11b, 2026-09-07) `PERFORMANCE.md` rows.** The sweep re-measured with the per-label pass (`the_retention_sweep_stays_inside_the_batch_budget` and its P18 note); a new measured number for the cap's cost on a new-series insert, which is the only path it touches.
- [ ] **CHANGELOG** entry, `PLAN.md` status, the arc-end adversarial review (`docs/REVIEW.md`), confirmed findings fixed.
**Acceptance:** `gridwatch config check` with `[sources.cpu] refres_ms = 1000` exits non-zero and prints the accepted set; with `[sources.weather]` for a configured plugin it exits 0 with a warning; `gridwatch --demo` with the same typo starts and toasts once. A store fed a `net` label that stops arriving holds no series for it after `max_age + sweep_every`, and holds every series for a label that keeps arriving, including its once-published scalars. `gridwatch keys` prints a `labels` column and `docs/KEYS.md` matches. The disjointness test names every component/source pair it checked.
**Performance gate:** P18's sweep number re-taken and still microseconds per batch on the boundary; no per-`apply` cost for catalogued keys (the cap looks up nothing unless a series is *created* for an uncatalogued name).
**Risks:** a `Dynamic` key whose label is legitimately quiet for ten minutes and then returns (a Wi-Fi interface that reconnects) is re-created from scratch — its chart starts empty, which is correct and should be said in the CHANGELOG; the plugin cap refusing samples a plugin author expected to land — the toast and the tile note are the whole mitigation.
**Status 2026-09-07 (built and gated; the adversarial review and the tag are the Fable session's).** All six deliverables shipped; the CHANGELOG box above stays open because it also covers the review. Three things this arc **found** rather than built. (1) **§9's disjointness rule is violated four times today**, and the brief assumed the generalised test would pass: `fps` is both `[sources.audio]`'s capture rate and the audio tile's animation rate, `chips` is both what the sensors source samples and what its tile shows, `art` is both the media source's cover-art fetch and winamp's art pane, and winamp's `fps` collides with the audio source's. Each is the exact ambiguity §9 exists to forbid, and the only mistake `check_sources` can never catch, because the name is valid on both sides. Renaming a documented config key edits §9's own example block, so it is a `DECISIONS.md` entry and not an implementation session's call: the test carries the four as an exact list that fails in both directions, and the rename is escalated. (2) **`[store] history` and `[store] max_mb` are parsed and never used** — `Shell::new` builds `Store::default()`, so the shipped `history = "10m"` is right by coincidence and setting `1h` does nothing; it is not in this arc's scope and is recorded here. (3) The performance gate's parenthetical is imprecise: the cap calls `key::lookup` on **every** series creation, catalogued or not — what it never does is look anything up for a *push* to a series that exists, which is what the steady-state claim needs and what the measurement shows (110 ns against 447 ns). The per-label sweep costs about fifteen times (debug against debug; 7.0 µs release) arc 10b's prune-only pass, because it builds the newest `Ts` per `(domain, label)` before it can evict anything; still 7.0 µs a batch amortised and ≈ 70 µs a sweep, so P18 is untouched.

## Arc 12 — v0.12.0 "the wide terminal"
*D62, 2026-09-06. Matt's bug report: tiles are mostly empty on a wide or tall terminal. Not the layout engine — three drawing rules inside components derive a size from the rect the wrong way, and a fourth is only a matter of time without a rule. Pulled ahead of arc 11's breadth at Matt's request. Built by an Opus session against `docs/briefs/arc-12.md`; no seam changes.*
**Goal:** a tile at 480×135 is as full as one at 250×70, and §4.6 says why it must be.
**Deliverables**
- [x] **The sparkline renderer holds** (2026-09-06). `ui::renderer::sparkline`: a column with no sample draws the newest sample before it; columns before the first sample stay empty. Every sparkline (htop, gpu, pins, net) is continuous at any width; the span rule is unchanged.
- [x] **The core bars fit the block** (2026-09-06). `htop::view::geom` takes the largest bar width that fits (no cap of 4) and centres the block, labels included.
- [x] **Bands come from the rect** (2026-09-06). `Gpu::band_rows`: the remainder above `table_rows` on the grid (floor 4, no ceiling), a third of the height when zoomed; htop's `cores` header quarter loses its ceiling of 8. §8.1 already says so.
- [x] **`assert_grows_with_area` in the testkit** (2026-09-06), run over every built-in component's listed tiers in `crates/components/tests/components.rs`; anything it catches beyond the three above is fixed the same way or escalated by name. It caught two more of the same family — `net`'s and `sensors`' sparkline/chart windows were a fixed span rather than the run's age, so both stopped part-way across their rect — and seven axes where the cell count is the wrong oracle for the mark (recorded with their numbers in the test's comment).
- [x] **The headless case** (2026-09-06). A cli test renders `shot --size 480x135` and asserts the non-blank fraction inside the CPU and GPU tiles; a named slot for Matt's real terminal size (`MACHINE.md`, still unknown).
- [x] **Snapshots regenerated and read** (2026-09-06), not just accepted: the 250×70 CPU tile's sparkline is continuous, the core bars unchanged where they already fit.
- [x] **`PERFORMANCE.md`** (2026-09-06): the uncached frame at 480×135 measured with the criterion bench beside P19's 513 µs at 250×70 — a number, not a new gate.
- [x] **CHANGELOG** entry (2026-09-06), `PLAN.md` status, the arc-end adversarial review (`docs/REVIEW.md`), confirmed findings fixed. The review and the tag are the Fable session's.
**Acceptance:** `gridwatch shot --size 480x135` shows a CPU tile whose sparkline has no interior gaps, whose core bars span each CCD half, and a GPU tile whose chart band is taller than 8 rows; the growth sweep passes for every listed tier; the 250×70 snapshots differ only where a gap closed.
**Performance gate:** P19 unchanged at 250×70; the 480×135 frame number recorded — **1.02 ms** uncached (2026-09-06), against 527 µs at 250×70 in the same run.
**Risks:** a held sparkline hides a *stale* source's flat tail — the `STALE` badge (arc 3b) is the signal for that and stays; a centred core block moves the id labels, which the PARITY row for htop's per-core layout must still accept.
**Status 2026-09-06 (reviewed, fixed, pushed).** Four lenses (correctness, spec-drift, ux-theme, user-path in a real pty at eleven sizes incl. 1×1 and 1000×300 — no panic anywhere, `q` restores every mode, no tty exits 1 with the message). Confirmed and fixed (D62 amendments): the pins watts sparkline capped its buckets at the sample count (the very cap §4.6 forbids, 41 of 158 columns blank at 480×135); the gpu 20 ms power trace was 50 columns at any width (stretched to the strip now); a plain zoomed third gave the gpu table *fewer* rows than the grid below 30 inner rows (the zoomed band is never taller than the grid's); the htop header's right column was `View::Empty` under `Fill` (a quarter of page 2 at 480×135 — it holds a MEM sparkline now); the pins `trend` tier's bar band and sparkline were fixed `Len`s (they share the rows above the floors); the sensors legend under-claimed by up to 59 s; the smoke test's tile finder panicked or measured six tiles as one in dense mode; a stale "arrive with the pins source (arc 3)" placeholder in the gpu `full` tier; and the doc drift the spec lens listed. Recorded, not fixed: the blank areas that remain at 480×135 and beyond (`BACKLOG.md`).

## Arc 13 — v0.13.0 "the config means what it says"
*D63, 2026-09-07. The two `P1` items arc 11's review escalated — a source's option *values* are never type-checked, and `[store] history`/`max_mb` are parsed and read by nothing — plus the three keys the backlog did not know were dead (`confirm_kill`, `[perf] phase_ms`, and `[record]` in the spec's own example). The seams (`SourceDef.check` with `OptionIssue`; `Retention::for_history`, `Store::retention`, `Store::footprint`) land first; everything else is breadth against `docs/briefs/arc-13.md`. Split by what reverts alone: **13a** touches `gridwatch-sources` and `app`; **13b** touches the store's construction, the loader and the docs.*
**Goal:** every key `config.toml` accepts is read, or says it is not — and a value that is quietly discarded is a failure, not a default.
**Deliverables**
- [x] **(13a) A source's option values are checked by the code that reads them.** One typed reader in `gridwatch-sources` behind every `Options::from_table`, returning the options *and* its issues; `SourceDef.check` hands the same list to `config check`. A **rejected** value (wrong type, below a floor, not one of the choices, an empty list where one is required) fails the check (non-zero) naming the source, the key, what was found, what was expected and what stands, and is toasted once at `run` while the source starts on its default; an **adjusted** value (clamped, or `chips` completed with `k10temp`) is a warning line and a toast. The cpu and gpu sources gain an `Options` type. Accepted shapes are never narrowed.
- [x] **(13a) The names/values seam is pinned both ways.** Per source, the reader's asked set equals `OPTION_NAMES` (replacing the `len()` tripwires); shell tests for a rejected and an adjusted value; pty **C.34** end to end, including `--demo`'s toast.
- [x] **(13b) `[store] history` is live.** `1m..=1h`, shipped `10m`; `max_age` from it and `max_len = max_age / 250 ms` (2 400 at the default — field-identical, so nothing deterministic moves); a non-duration is a load error naming the file; out of range clamps with a warning; a reload says "restart to apply"; `config check` prints the resolved numbers.
- [x] **(13b) Three keys retire and say so; `[record]` tells the truth; loader warnings reach the screen.** `max_mb`, `[perf] phase_ms` and `confirm_kill` parse for one more minor as `Option`s with a warning line and a toast, leave `config default`, and are `deprecated` in the schema; `[record]`'s warning names `--record`; every `loaded.warnings` line is toasted at start, not only logged.
- [x] **(13b) The store's footprint is a number.** `Store::footprint()` on the `F12` HUD and as `store_bytes` in the stats log — a measurement, never a cap; §4.2 and §13 stop claiming 32 MB.
- [x] **(13b) The config audit test.** Every leaf key of the embedded default has a row that mutates it and asserts what changed, so a key cannot be added without a consumer or a retirement; plus the three-way pin that the shipped TOML, the structural default and `Retention::default()` agree.
- [ ] **`PERFORMANCE.md`**: P17 at `history = "1h"` (a 60-minute run, RSS and `store_bytes`), P5's rows reworded to the mechanism that exists; **CHANGELOG** entry, `PLAN.md`, `BACKLOG.md` (both `P1`s struck, the `Ring::new` preallocation item added), the arc-end adversarial review (`docs/REVIEW.md`) with a lens that greps every `check` for I/O, confirmed findings fixed.
**Status 2026-09-07 (built and gated; the adversarial review and the tag are the Fable session's).** All six deliverables shipped, in five scoped commits, 13a before 13b. Three things worth recording. (1) **Nine of the seven sources' millisecond keys used to treat `0` two different ways**: cpu and gpu discarded it and kept their defaults, while pins, sensors, mpris and net clamped it *up* to their range floor — so `refresh_ms = 0` meant "the default" in one source and "as fast as allowed" in another. D63 names `refresh_ms = 0` as a rejection, so the reader rejects `<= 0` uniformly and clamps only a positive value that is out of range; the alternative preserved a per-source inconsistency nobody could have predicted from the config. (2) **`rapl` accepts six spellings, not D63's four** (`on`, `off`, `yes`, `no`, `true`, `false`): `off`/`no`/`false` were the three the old code consumed and the other three fell through to `true`, which is what they meant — refusing them would narrow a shape that worked, which trap 4 forbids. Only a word that meant nothing (`rapl = "maybe"`) is refused. (3) **`net` read its option keys in a different order from the one it declared**, which no test could see until the ask-set tripwire pinned `Reader::asked() == OPTION_NAMES` in order; the reads are independent, so the fix was behaviour-identical. Two additions beyond the brief's list: `Reader::rejected` (a caller-composed rejection, for `sink`'s "a serial must not be negative", which no per-key rule in the reader could express) and `app::check_sources`/`SourceReport` widened from `pub(crate)` to `pub` so the shell tests can assert the failure/warning split that decides the exit code. **P17 at the `1h` ceiling is owed to Matt** — see `PERFORMANCE.md`.

**Acceptance:** `gridwatch config check` with `[sources.cpu] refresh_ms = "1500"` exits non-zero and prints ``` `refresh_ms` expects an integer (milliseconds), found a string ("1500") — the default 1500 stands ```; with `refresh_ms = 50` it exits 0 with `clamped to 200`; with `[sources.pins] source = "i2x"` it names `auto, i2c, exporter`; `gridwatch --demo` with the string value starts and toasts once. `[store] history = "1h"` gives a store whose `retention().max_age` is 3600 s and `max_len` 14 400; `history = "ten"` refuses to start naming `config.toml`; a config carrying `max_mb = 32` still runs and says the key is retired. The shipped default produces no issue of any kind. `replaying_a_fixture_twice_is_byte_identical` is unchanged and green; every 250×70 snapshot is byte-identical to arc 12's.
**Performance gate:** P18 unchanged (the default `Retention` is field-identical); P17 measured at the `1h` ceiling and inside 60 MB; `footprint()` never runs per frame.
**Risks:** a config that ran yesterday failing today because a value it carried was always discarded — the message says what stood, which is what it should have said all along; a short `history` evicting a paused dashboard's `net`/`sensor` labels after `max_age`, D61's risk row made sharper (CHANGELOG); the reader narrowing a shape by accident (`sink`, `rapl`, floats) — the reader is the contract (D63 E1) and the review reads all seven.

## Arc 14 — v0.14.0 "what the drives are doing"
*D64, 2026-09-09. `BACKLOG.md`'s only `P1` and the last vertical in the plan nobody has read — nothing in gridwatch opens `/proc/diskstats` today. Deliberately **not** a seam arc: a new source and a new component against the existing traits, which §15 documents as the extension path. Fable was rate-limited, so an Opus session wrote D64, the ARCHITECTURE edits and `docs/briefs/arc-14.md`. Split by what reverts alone: **14a** touches `gridwatch-store` and `gridwatch-sources`; **14b** touches `gridwatch-components` and the generated docs.*
**Goal:** the tile answers "which drive is busy, how hard, and how much is that hurting" — and refuses to print a `%util` that would say 100 % about a drive at a sixth of its queue.
**Deliverables**
- [x] **(14a) The `disk` source.** One `/proc/diskstats` read per tick, rates from the measured interval with `saturating_sub` on a reset, at `2 s / 1 s / 500 ms`. Eleven keys: read/write/discard bytes per second, read and write IOPS, `busy_pct`, `queue`, the two awaits, `disk.info` (published on change) and `disk.scan_ms`. Every labelled key is `Dynamic` (D61). **No aggregate keys** — a "total" changes meaning under `extra` and disagrees with a filtered tile.
- [x] **(14a) A drive is a sysfs fact.** A `/sys/block` entry with a `device` link, `hidden == 0` and `size > 0` (3 of 55 on torch); partitions under `partitions = true`; `extra` globs for `dm-*`, `md*`, `zram0`; a hard cap of 16 published devices, drives first, with what was refused in `SourceStatus.reason`. Classification is lazy and cached per name with no rewalk timer, so a USB drive appears on the next tick.
- [x] **(14a) Options through arc 13's reader.** `refresh_ms` / `partitions` / `extra` behind `Reader`, registered as `SourceDef.check`, with the ask-set tripwire. Feature `disk` in all four crates, the four `default` lists and CI's matrix. `fixtures/diskstats/torch/` and `demo::DiskSynth` (one busy drive, one idle so the `—` await path has data, and `disk.info.device` matching `demo::sensors_info()`'s `nvme0/1/2` so the join is testable with no hardware). `KEYS.md` regenerated.
- [x] **(14b) The `disk` tile, five tiers.** `rates` 8×3 · `sparks` 20×5 · `table` 36×8 · `chart` 56×14 · `full` 100×24 zoom-only, with the column-drop order `Q, W/S, R/S, °C, BUSY` and an elastic `MODEL` so a wide tile carries content rather than stretched numbers. **`demand(tier)` is `Detail::Meters` at every tier, `full` included.**
- [x] **(14b) `BUSY`, never `%util`.** `Q` (the mean I/Os in flight) drawn beside it, the awaits as first-class keys drawn under §11's 3 × cadence rule, and the `full` pane printing `busy 100% · q 172 of 1023 · r 0.34 ms · w 9.5 ms` from the live interval, with the sentence that says what field 13 cannot tell you. Field 12 is never published.
- [x] **(14b) The temperature comes from the sensors source, joined by device.** `disk.info.device` against `ChipInfo.device`, then `sensor.temp_c{<chip>:Composite}`; hwmon is never re-read; `—` with the reason when the sensors source is absent, no chip matches, or a SATA drive has no `drivetemp`. `optional_sources = [sensors]`.
- [x] **(14b) `PARITY.md`.** htop's **DiskIO** row becomes `in — arc 14` with htop's loop-device inclusion recorded as a deviation; the **NetworkIO** row's stale text is **corrected** and honestly downgraded to `partial`; a new `disk` section against `iostat -x`. The header-meter breadth item stays `P2` and out.
- [ ] **`PERFORMANCE.md`** P23 (new) plus P5, P17, P1, P6, P8 and P19 re-taken; **CHANGELOG**, `PLAN.md`, `BACKLOG.md` (the `P1` struck; filesystem capacity, SMART, htop's NetworkIO sum and the diskstats merge fields added), `COMPONENTS.md` and `KEYS.md` regenerated, `LAYOUT.md`'s placement example, the arc-end adversarial review with a lens that greps the component for `std::fs` and for `Detail::`, confirmed findings fixed. *(Everything here is done except the review and its fixes — the box stays open until they are.)*
**Acceptance:** `gridwatch keys` prints eleven `disk.*` rows, all labelled ones `dynamic`; on torch the source publishes exactly `nvme0n1`, `nvme1n1`, `nvme2n1` and **no loop device**; `extra = ["loop*"]` publishes 16 and the `sources` tile says how many it refused; `partitions = true` adds the nine partitions and stays inside the cap; `refresh_ms = "1000"` exits `config check` non-zero with D63's sentence. A sandbox `layout.toml` under `--demo` draws three drives with rates, `BUSY`, `Q` and a temperature per drive, and `1`–`4` change the chart series; the same tile built `--no-default-features --features disk` draws `—` for every temperature and says why in `full`. The growth sweep passes for every listed tier and the tier-per-rect test matches §8 at all seven sizes. **The disjointness test names `disk×disk` and `disk×sensors` and adds nothing to `KNOWN`.** `replaying_a_fixture_twice_is_byte_identical` unchanged and green.
**Performance gate:** **P23** — the pass ≤ 1 ms wall and ≤ 0.2 % of one core at 1 s, with `disk.scan_ms` as the evidence, and classification once per device name rather than per tick. **P5 is the row that can break** — 33 wake-ups/s measured with every source live against a ceiling of 40; measure it with the tile visible, do not assert it, and if it exceeds 40 the cadence is the knob. **P17** re-taken, with the arithmetic recorded: 1.0 MB for three drives, 4.2 MB with partitions, 5.5 MB at the cap, **22 MB unfiltered** against a measured 40.3 MB of 60. P13 and P15 unchanged and asserted so.
**Risks:** **P5** has seven wake-ups of headroom and this adds a 1 Hz source; if it goes over, the visible cadence drops to 2 s and the row is re-taken. **`extra` spends P17's headroom** — the 16-device cap is the whole mitigation, and the CHANGELOG must say `extra = ["loop*"]` on a snap-heavy box costs several megabytes of store. **The cross-source join is the arc's one genuinely new mechanism**, mitigated by the demo synth exercising it in CI; a machine whose hwmon layout differs will show `—`, which is correct and must read as correct. **A drive that vanishes and returns** is re-created from scratch after `max_age` with an empty chart — D61's risk row, true here too. **Where `disk` lives in the shipped default layout is Matt's call**: the Overview is full and every candidate slot displaces a tile a test or the performance protocol depends on, so arc 14 does not touch `layout.toml`.

**As built (2026-09-09, Opus).** Every box above is done except the two halves
of the last one that were never this session's: the **adversarial review** is
the next session's, and the tag is Matt's. Four things went differently from
the brief and are recorded rather than smoothed over:

1. **The diskstats length guard is per group, not `>= 20`.** The layout grew
   twice — 11 stats before 4.18 (14 fields), 15 with the discard group (18),
   17 with the flush group (20) — so a `>= 20` guard would have silently
   dropped `disk.discard_bps` on a real 4.18–5.4 kernel that has it, and the
   brief's "17-field pre-5.5" fixture is a shape no kernel emits. The core
   needs 14 fields, `discard_bps` needs the whole discard group, and a longer
   tail is ignored. **`ARCHITECTURE.md` §8's degrade sentence carried the
   brief's claim and has been corrected to the three shapes** — a factual
   correction about kernel history, not a seam change, and one D64 may want
   to amend.
2. **`sources/Cargo.toml`'s `default` list does not gain `disk`.** That list
   is `["cpu", "gpu", "pins"]` and has never carried `net` or `sensors`
   either; the three that matter (`components`, `app`, `cli`) all gained it,
   as did CI's feature matrix.
3. **P5 is over its ceiling and the disk source is not why** — 0.5 wake-ups/s
   of a measured 70. `PERFORMANCE.md` isolates the two causes, both older than
   this arc.
4. **The growth sweep asserts three of the five tiers.** `sparks` both ways,
   `table` and `chart` by width; the four excluded axes are named with their
   measured ratios in the test's comment, as arc 12 did.
